// src/enums/response.enum.ts

export enum ResponseCodeEnum {
  SUCCESS = 0,
  ERROR = 1,
  // 可以添加更多自定义状态码
}

export enum ResponseMessageEnum {
  SUCCESS = 'Success',
  ERROR = 'Error',
  // 可以添加更多自定义消息
}